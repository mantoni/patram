# Check

- Command: check
- Command Summary: Validate a project, directory, or file and report graph
  diagnostics.

`patram check [<path>]` validates configuration, parsed sources, and broken
document links for the selected scope.
