# Document

- Term: document
- Term Definition: The built-in file-backed graph node kind keyed by normalized
  relative path.

Each scanned source file contributes one `document` node even when no other
claims materialize from that file.
