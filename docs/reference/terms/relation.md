# Relation

- term: relation
- definition: A graph edge type between nodes.

Relations such as `links_to` or `uses_term` connect source documents to other
documents or taxonomy nodes. In Patram v2, user-authored relations come from
`ref` fields rather than a separate `relations` config section.
